# Cold review 2

## Decision

Accept with constraints.

## Admissibility

The second candidate fixes the mutable-directory identity defect without
weakening replacement detection, proves one exact digest-bound Rspamd
install/replacement/reopen/rollback lifecycle against canonical Mail release
artifacts, retains durable Bayes state, and passes the focused hostile, race,
repeat, shuffle, coverage, vet, and full repository gates.

The five role constructors remain typed. Callers cannot inject a command,
argument, mount, port, capability, environment variable, image pull, shell,
or arbitrary health probe. Effective inspection rejects drift. Mutations are
separate and idempotent only at their exact successor state.

## Constraints

- This admits private runtime mechanism only.
- Rspamd is the real representative replacement proof; all five exact product
  roles pass the separate combined Mail smoke, but the Stack controller has not
  coordinated the complete topology.
- The controller, identity/provider composition, public artifacts, DNS/PTR,
  backup custody, monitoring destination, and full lifecycle acceptance remain
  unfinished.
- No live target or mutation authority may be inferred from this admission.

## Final ruling

Accepted. The runtime-adapter child is complete; the journal-bound controller
is the next active boundary.
