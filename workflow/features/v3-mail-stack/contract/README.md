# Mail stack contract

State: `in_progress`

Defines the production release artifact, fixed listener and certificate
ownership, five role authorities, file-only secret transport, persistent-state
and backup boundaries, lifecycle/recovery state machine, and verification
matrix before any Stack apply authority is introduced.
