# V0 plan kernel

Active foundation feature. It owns strict parsing, dependency planning,
deterministic SHA-256 identities, the read-only CLI, hostile tests, and
admission evidence. It owns no external mutation.
