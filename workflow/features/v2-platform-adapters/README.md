# V2 platform adapters

The original item was too broad for one reviewable critical feature. It is
split into separately admitted Caddy, Authentik, and PostgreSQL children.

The active Caddy child is documented in `caddy/`. Its completion does not mark
this parent done: Authentik and PostgreSQL remain unresolved, and no controller
apply command is admitted until all required adapter and recovery boundaries
have their own disposable evidence.
