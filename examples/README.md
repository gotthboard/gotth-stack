# Examples

`full-stack.json` is a non-deployable manifest example. Every artifact URL and
digest is a placeholder. It contains no credential values and grants no
authority. Use it only to exercise `validate` and `plan`.
